pipeline {

    agent any

    stages {

        stage('Install Dependencies') {

            steps {

                bat 'python -m pip install -r "Automation Testing/requirements.txt"'
            }
        }

        stage('Clean Previous Reports') {

            steps {

                bat 'if exist "Automation Testing\\reports\\allure-results" rmdir /s /q "Automation Testing\\reports\\allure-results"'
            }
        }

        stage('Run API Tests') {

            steps {

                catchError(
                    buildResult: 'UNSTABLE',
                    stageResult: 'FAILURE'
                ) {

                    bat 'python -m pytest "Automation Testing/tests/api" -n 2 --alluredir="Automation Testing/reports/allure-results/api"'
                }
            }
        }

        stage('Run Hybrid E2E Tests') {

            steps {

                catchError(
                    buildResult: 'UNSTABLE',
                    stageResult: 'FAILURE'
                ) {

                    bat 'python -m pytest "Automation Testing/tests/e2e" --alluredir="Automation Testing/reports/allure-results/e2e"'
                }
            }
        }

        stage('Run UI Tests') {

            steps {

                catchError(
                    buildResult: 'UNSTABLE',
                    stageResult: 'FAILURE'
                ) {

                    bat 'python -m pytest "Automation Testing/tests/ui" --alluredir="Automation Testing/reports/allure-results/ui"'
                }
            }
        }

        stage('Generate Allure Report') {

            steps {

                allure(

                    includeProperties: false,

                    jdk: '',

                    results: [

                        [
                            path: 'Automation Testing/reports/allure-results/api'
                        ],

                        [
                            path: 'Automation Testing/reports/allure-results/e2e'
                        ],

                        [
                            path: 'Automation Testing/reports/allure-results/ui'
                        ]
                    ]
                )
            }
        }
    }

    post {

        always {

            archiveArtifacts(

                artifacts: 'Automation Testing/reports/screenshots/*.png',

                allowEmptyArchive: true
            )
        }

        success {

            echo 'Pipeline executed successfully.'
        }

        unstable {

            echo 'Some test cases failed. Build marked as UNSTABLE.'
        }

        failure {

            echo 'Pipeline execution failed.'
        }
    }
}
